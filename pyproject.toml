[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "rag-ai-assistant"
version = "1.0.0"
description = "Production-ready RAG AI Assistant using LangChain, OpenAI, and Pinecone"
requires-python = ">=3.10"
